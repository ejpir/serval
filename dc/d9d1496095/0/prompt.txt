• Findings

  1. High: Data race/UB in health counters between request path and prober thread
      - Passive updates write health state from request handling via serval-lb/handler.zig:141 -> serval-lb/strategy_core.zig:59.
      - Active probing writes the same health state from background thread via serval-prober/scheduler.zig:43 -> serval-prober/scheduler.zig:53.
      - HealthState counters are plain mutable arrays (failure_counts, success_counts) not atomic/locked (serval-health/health_state.zig:24, serval-health/health_state.zig:27, writes at serval-health/
        health_state.zig:77, serval-health/health_state.zig:100).
      - This violates enterprise safety expectations (S1/S4/C4) under concurrent load.
  2. High: upstream.idx contract is not validated at LB init, can corrupt health mapping
      - Selection is by array position (serval-lb/strategy_core.zig:47).
      - Health updates use entry.upstream.idx (serval-lb/handler.zig:147).
      - No init-time validation ensures each upstream’s idx is dense/local (0..upstreams.len-1) in serval-lb/handler.zig:73.
      - In release builds, relying on asserts only is risky for misconfiguration (S7/C3/C5).
  3. Medium: TLS probe misconfiguration is silent, causing permanent unhealthy state


      - LbHandler docs say TLS probes require client_ctx when any upstream has tls=true (serval-lb/handler.zig:68), but init does not enforce it.
      - Probe failures are swallowed as false in adapter catches (serval-prober/adapters.zig:134, serval-prober/adapters.zig:40).
      - serval-client explicitly fails TLS connect when client_ctx == null (serval-client/client.zig:247).
      - Result: backends may never recover, with limited observability (C4).
  4. Medium: Prober shutdown latency can block deinit() up to full probe interval
      - Scheduler sleeps fixed interval per cycle (serval-prober/scheduler.zig:37).
      - deinit() sets flag then blocks on join() (serval-lb/handler.zig:120, serval-lb/handler.zig:124).
      - With large intervals this slows graceful shutdown/drain (operability/perf concern).
  5. Low: serval-lb README API examples are stale/inaccurate
      - Usage example calls handler.init with old signature (serval-lb/README.md:44); current API requires client_ctx and dns_resolver (serval-lb/handler.zig:73).
      - Struct snippet shows fields that no longer match implementation (serval-lb/README.md:57 vs actual serval-lb/handler.zig:55).

  Open questions / assumptions

  1. Assumed LbHandler.onLog() and prober run concurrently for the same handler instance (consistent with current thread model).
  2. Assumed upstream.idx is expected to be local-per-pool index, not merely an opaque identifier.

---

Base directory for this skill: /home/nick/.claude/plugins/cache/superpowers-marketplace/superpowers/4.0.3/skills/writing-plans

# Writing Plans

## Overview

Write comprehensive implementation plans assuming the engineer has zero context for our codebase and questionable taste. Document everything they need to know: which files to touch for each task, code, testing, docs they might need to check, how to test it. Give them the whole plan as bite-sized tasks. DRY. YAGNI. TDD. Frequent commits.

Assume they are a skilled developer, but know almost nothing about our toolset or problem domain. Assume they don't know good test design very well.

**Announce at start:** "I'm using the writing-plans skill to create the implementation plan."

**Context:** This should be run in a dedicated worktree (created by brainstorming skill).

**Save plans to:** `docs/plans/YYYY-MM-DD-<feature-name>.md`

## Bite-Sized Task Granularity

**Each step is one action (2-5 minutes):**
- "Write the failing test" - step
- "Run it to make sure it fails" - step
- "Implement the minimal code to make the test pass" - step
- "Run the tests and make sure they pass" - step
- "Commit" - step

## Plan Document Header

**Every plan MUST start with this header:**

```markdown
# [Feature Name] Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** [One sentence describing what this builds]

**Architecture:** [2-3 sentences about approach]

**Tech Stack:** [Key technologies/libraries]

---
```

## Task Structure

```markdown
### Task N: [Component Name]

**Files:**
- Create: `exact/path/to/file.py`
- Modify: `exact/path/to/existing.py:123-145`
- Test: `tests/exact/path/to/test.py`

**Step 1: Write the failing test**

```python
def test_specific_behavior():
    result = function(input)
    assert result == expected
```

**Step 2: Run test to verify it fails**

Run: `pytest tests/path/test.py::test_name -v`
Expected: FAIL with "function not defined"

**Step 3: Write minimal implementation**

```python
def function(input):
    return expected
```

**Step 4: Run test to verify it passes**

Run: `pytest tests/path/test.py::test_name -v`
Expected: PASS

**Step 5: Commit**

```bash
git add tests/path/test.py src/path/file.py
git commit -m "feat: add specific feature"
```
```

## Remember
- Exact file paths always
- Complete code in plan (not "add validation")
- Exact commands with expected output
- Reference relevant skills with @ syntax
- DRY, YAGNI, TDD, frequent commits

## Execution Handoff

After saving the plan, offer execution choice:

**"Plan complete and saved to `docs/plans/<filename>.md`. Two execution options:**

**1. Subagent-Driven (this session)** - I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Parallel Session (separate)** - Open new session with executing-plans, batch execution with checkpoints

**Which approach?"**

**If Subagent-Driven chosen:**
- **REQUIRED SUB-SKILL:** Use superpowers:subagent-driven-development
- Stay in this session
- Fresh subagent per task + code review

**If Parallel Session chosen:**
- Guide them to open new session in worktree
- **REQUIRED SUB-SKILL:** New session uses superpowers:executing-plans

---

conr

---

• 1. High: Task 3 test cannot work with current ordering; it will trip the existing assert before returning TlsContextRequired
      - The plan’s test uses .enable_probing = true and dns_resolver = null and expects error.TlsContextRequired (docs/plans/2026-03-29-lb-health-findings-fixes.md:282, docs/plans/2026-03-29-lb-health-
        findings-fixes.md:288, docs/plans/2026-03-29-lb-health-findings-fixes.md:291).
      - But LbHandler.init currently asserts !enable_probing or dns_resolver != null first (serval-lb/handler.zig:85).
      - Plan says add TLS validation “after idx validation” (docs/plans/2026-03-29-lb-health-findings-fixes.md:313), which is still after that assert. In debug this panics, not error-returns.
  2. High: Narrowing init to LbError!void is incompatible with std.Thread.spawn error propagation
      - Plan requires changing signature to LbError!void (docs/plans/2026-03-29-lb-health-findings-fixes.md:239).
      - init does try std.Thread.spawn(...) (serval-lb/handler.zig:113), which returns spawn-related errors not in LbError.
      - Without explicit mapping or union error set, this won’t compile.
  3. Medium: Proposed scheduler code violates repo hard rule No catch {}
      - Plan’s interruptibleSleep uses std.Io.sleep(... ) catch {}; (docs/plans/2026-03-29-lb-health-findings-fixes.md:424).
      - AGENTS contract explicitly disallows catch {} in production code.
  4. Medium: Verification command mismatch for Task 4
      - Task 4 modifies serval-prober/scheduler.zig (docs/plans/2026-03-29-lb-health-findings-fixes.md:347).
      - It then verifies with zig build test-health (docs/plans/2026-03-29-lb-health-findings-fixes.md:432), which does not directly target prober tests. This weakens regression coverage.
  5. Low: “Write failing test” claim for Task 1 is not actually a deterministic failing test
      - Plan acknowledges race test may pass and only “documents pattern” (docs/plans/2026-03-29-lb-health-findings-fixes.md:84).
      - For enterprise-grade gatekeeping, this should be framed as stress/smoke, not fail-first proof.

---

• 1. High: Task 3 test is invalid as written due to existing precondition assert
      - Plan expects error.TlsContextRequired with probing enabled and dns_resolver = null (docs/plans/2026-03-29-lb-health-findings-fixes.md:282, docs/plans/2026-03-29-lb-health-findings-fixes.md:288,
        docs/plans/2026-03-29-lb-health-findings-fixes.md:291).
      - Current code asserts probing requires resolver first (serval-lb/handler.zig:85).
      - So debug builds will panic before returning the planned TLS error unless check ordering changes.
  2. High: init error-set change to LbError!void conflicts with Thread.spawn
      - Plan says change init to LbError!void (docs/plans/2026-03-29-lb-health-findings-fixes.md:239).
      - init currently trys std.Thread.spawn (serval-lb/handler.zig:113), whose errors are outside LbError.
      - This will require explicit mapping or a wider error union, otherwise it won’t compile.
  3. Medium: Plan introduces forbidden catch {}
      - Proposed scheduler patch uses std.Io.sleep(...) catch {}; (docs/plans/2026-03-29-lb-health-findings-fixes.md:424).
      - AGENTS contract explicitly forbids catch {}.
  4. Medium: Task 4 verification command does not match modified module
      - Task modifies serval-prober/scheduler.zig (docs/plans/2026-03-29-lb-health-findings-fixes.md:347).
      - It verifies with zig build test-health (docs/plans/2026-03-29-lb-health-findings-fixes.md:432) rather than test-prober/full test.
  5. Low: Task 1 “failing test” is not deterministic fail-first
      - Plan notes the concurrency test may pass and mainly documents usage pattern (docs/plans/2026-03-29-lb-health-findings-fixes.md:84).
      - That’s fine as stress coverage, but should not be described as a strict fail-first gate.

---

• 1. High: Verification commands can mask test failures
      - Multiple steps use pipelines like zig build ... 2>&1 | tail -20 and ... | grep ... (docs/plans/2026-03-29-lb-health-findings-fixes.md:83, docs/plans/2026-03-29-lb-health-findings-fixes.md:223,
        docs/plans/2026-03-29-lb-health-findings-fixes.md:304, docs/plans/2026-03-29-lb-health-findings-fixes.md:388, docs/plans/2026-03-29-lb-health-findings-fixes.md:505).
      - Without set -o pipefail, pipeline exit status is from tail/grep, not zig build. This is not enterprise-safe verification.
  2. Medium: Single global mutex in HealthState is a potential hot-path bottleneck
      - Plan introduces one counter_mutex protecting all counter updates (docs/plans/2026-03-29-lb-health-findings-fixes.md:95, docs/plans/2026-03-29-lb-health-findings-fixes.md:130, docs/plans/2026-03-29-
        lb-health-findings-fixes.md:152).
      - Correctness improves, but this serializes all passive/prober updates across all backends. For high-throughput LB, that may regress performance and tail latency.
  3. Medium: Shutdown-latency test does not validate the new interruptible sleep behavior
      - Proposed test only covers probe_running = false at entry (docs/plans/2026-03-29-lb-health-findings-fixes.md:349, docs/plans/2026-03-29-lb-health-findings-fixes.md:352).
      - It does not exercise the key path where the loop is sleeping and the flag flips to false mid-interval.

  Overall: plan is much better now and mostly implementable; the main blocker is verification rigor (pipeline masking), plus two residual performance/test-depth risks.